[package]
name = "mecha"
version = "0.1.0"
edition = "2021"

[dependencies]
    # third party
        # serde
            serde = { version = "1.0", features = ["derive"] }
            serde_json = "1.0"
    # graph
        graph = { path = "../graph" }
        text = { path = "../text" }