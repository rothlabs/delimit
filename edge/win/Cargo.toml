[package]
    name = "win"
    version = "0.1.0"
    edition = "2021"

[dependencies]
    anyhow = { workspace = true }
    wgpu = "22.1.0"
    winit = "0.30"
    tokio = { version = "1", features = ["full"] }
    graph = { path = "../../core/graph" }
    gpu = { path = "../gpu" }