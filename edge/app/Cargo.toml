[package]
name = "app"
version = "0.1.0"
edition = "2021"

[dependencies]
    tokio = { version = "1", features = ["full"] }
    winit = "0.30"
    wgpu = "22.1.0"
    win = { path = "../win" }
    gpu = { path = "../gpu" }
    graph = { workspace = true }
    mech = { workspace = true }
